# CLAUDE.md

**All agent rules live in [`AGENTS.md`](AGENTS.md) — read it in full before any task.**
It is the single source of truth shared by every AI coding tool used on this project
(Claude Code, Codex, Cursor). Do not duplicate rules here; propose changes there.

Quick orientation: product = `docs/PRD.md` · architecture = `docs/adr/ADRs.md` ·
current work = `docs/sprints/` · feature specs = `docs/specs/` ·
UI = `docs/design/UI-SPEC.md` + `docs/design/august-ui/`.
